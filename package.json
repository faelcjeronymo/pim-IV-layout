{
  "name": "pim-layout",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server ./ -d false -P http://localhost:8080/segurados/ --proxy-options.ignorePath true --proxy-options.autoRewrite true"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.2.2",
    "http-server": "^14.1.1",
    "jquery": "^3.6.1"
  }
}
